# Reviewer-Selbst-Lessons (ki-investment)

- 2026-07-19 (S-074, Iter.1) — **Bündelt ein AC sowohl eine hart testbare Architektur-/Sicherheits-Invariante (z. B. „Control-POSTs laufen ausschliesslich über Datei X") ALS AUCH ein konkretes Response-Format/UI-Detail (z. B. „... und liefert ein HTML-Partial zurück"), und der Coder liefert nur den ersten Teil mit Verweis auf spätere View-Storys — vor dem Akzeptieren als legitimer Scope-Cut IMMER `board show <folge-story-id>` für JEDE genannte Folge-Story prüfen, ob sie dieselbe AC-Nummer tatsächlich in `implements` führt.** Bei S-074 (AC20/AC21, Control-Endpoints) verwies der Coder auf S-075/S-076 als künftige „View-Anbindung" — `board show S-075`/`S-076` zeigte aber `implements: ["AC17","AC19"]` bzw. `["AC18"]`, NICHT AC20/AC21. Ohne diesen Board-Check hätte der JSON-only-Cut wie ein sauber sequenzierter Multi-Story-Rollout ausgesehen, obwohl in Wahrheit keine Story den „liefert Partial zurück"-Teil der AC beansprucht (Traceability-Lücke). Das rechtfertigt Critical + „über `requirement` klären" statt PASS.
- 2026-07-12 (S-027, Iter.1) — **Secret-Sync-Gate Zeile 3 (`.env.gpg` unverändert nach neuer `.env.example`-Variable) ist in diesem Projekt für explizit als "Kein Secret-Wert" markierte Konfig-Skalare mit Code-Default ein etabliertes, akzeptiertes Muster — kein Blocker.** `TOLERANZ_CONFIG` (S-013) wurde genauso eingeführt (auskommentiert, "Kein Secret-Wert", Default in `app.config`) und `.env.gpg` wurde dabei nicht angefasst; die Story ist gemergt. `HALLUZINATIONS_KPI_SCHWELLWERT` (S-027) folgt demselben Muster. Bei neuen `.env.example`-Einträgen zuerst `git log -p -- .env.example` auf ein Präzedenzmuster prüfen, bevor die Heuristik als Important gemeldet wird — sonst falsch-positiver Blocker gegen etablierte Projekt-Konvention (nur echte Secrets landen in `.env.gpg`, Konfig-Skalare mit sicherem Default nicht).
- 2026-07-12 (S-016, Iter.1) — **Vor dem Melden von "`.env.gpg` nicht re-encrypted" (Secret-Sync-Gate Signal 3) immer erst `git log -p -- .env.example` auf Präzedenzfälle prüfen — bestätigt hier erneut für `EINSTAND_METHODE_DEFAULT` (S-016, gleiches Muster wie TOLERANZ_CONFIG/HALLUZINATIONS_KPI_SCHWELLWERT): Konfig-Skalar mit sicherem Code-Default + "Kein Secret-Wert"-Kommentar ist etabliertes Projekt-Muster, kein Blocker.**
- 2026-07-19 (S-071, Iter.2) — **Best-Practice-Bestätigung, kein Fehl-Call:** eine "SPEC-LÜCKE"-Meldung des Coders (Critical Iter.1: Depot-Verlauf-Chart strukturell nicht baubar, kein Zeitreihen-Read-Modell) sauber via `/requirement` aufgelöst geprüft — Spec v2→v3 zog AC14 explizit zurück (kein stilles Streichen: `@trace`-Referenz AC14→S-081→AC32/AC33, Verträge-Tabelle + Nicht-Ziele + Abhängigkeiten alle nachgezogen, neue Story S-081 schema-konform inkl. `board.yaml`/F-017-Rollup). Checkliste für künftige ähnliche Fälle: (1) AC-Text selbst verweist auf die Trace-Kette, (2) Verträge-Tabelle bekommt eine neue Zeile für den ausgelagerten Teil, (3) Nicht-Ziele-Abschnitt hält den Auslagerungsgrund fest, (4) Story-YAML hat `depends` auf die Restlaufzeit-Voraussetzungen, (5) `board.yaml next_story_id` + Feature-Rollup (`progress`/`stories[]`) konsistent. Alle 5 Punkte erfüllt → PASS, kein Downgrade nötig.
- 2026-07-19 (S-078, Iter.1) — **Best-Practice-Bestätigung, kein Fehl-Call:** MinTRL-Restlaufzeit additiv angebunden — `LetztesGateErgebnis.min_trl`/`SystemStatusResponse.mintrl_restlaufzeit` als Default-`None`-Felder ohne Signaturbruch, Tage→Jahre-Umrechnung sauber als "reine Anzeige-Formatierung" (kein neuer Rechenpfad, AC24-Boundary) begründet und mit nicht-rundem Testwert (python/R14) belegt, keine bestehenden `data-testid`s umbenannt (S-077-Playwright-Anker unversehrt). PASS ohne Downgrade.
